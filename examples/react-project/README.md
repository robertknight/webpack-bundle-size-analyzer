This is an example project that shows how to use webpack-bundle-size-analyzer
as a Webpack plugin.

## Usage

1. Build the JavaScript bundle:

   ```
   npm install
   npx webpack
   ```

2. View the dependency size report in `dist/dependency-size-report.txt`.

